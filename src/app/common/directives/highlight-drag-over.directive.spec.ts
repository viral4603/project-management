import { HighlightDragOverDirective } from './highlight-drag-over.directive';

describe('HighlightDragOverDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDragOverDirective();
    expect(directive).toBeTruthy();
  });
});
